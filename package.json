{
  "name": "qr-redirect",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "seed:kv": "node scripts/seed-kv.js",
    "test": "node --test",
    "test:wrangler": "wrangler dev --test-scheduled"
  },
  "devDependencies": {
    "wrangler": "^4.99.0"
  }
}
