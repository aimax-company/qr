{
  "name": "qr-redirect",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "node --test",
    "test:wrangler": "wrangler dev --test-scheduled"
  }
}